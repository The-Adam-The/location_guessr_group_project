import './Header.css';

const Header = () => {
    return(
        <header>
            <nav>
                <a className="logo" href="/">WHEREABOUTS</a>
            </nav>
        </header>
    )
};

export default Header;